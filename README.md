# API SageAPP Services

## A API já está hospedada no Render, não funcionará caso tente roda-la localmente pois não tem as variáveis de ambiente!

# Como rodar localmente para testes:

```
npm install
npm run dev
```
